import sdk from "@api/sdk";
import {IResult} from "@/types";


export interface IAreaItem {
  id: number;
  mname: string;
  code?: any;
  lever: number;
  fid: number;
  create_time: Date;
  mod_time: Date;
  flag: number;
  type: number;
  company?: any;
}


<%=tsDefinded%>

<% webapiGroup.apis.forEach(apiItem=>{ %>

/**
 *
 * <%=apiItem.comment%>
 *
 *  TODO
 */
async function <%=apiItem.name%>(
    <% apiItem.requestParam.forEach(paramItem=>{ %>
        <%=paramItem.name%>:<%=Util.genInterfaceName(apiItem.name,paramItem.name,"req")%>,
    <% }) %>
):Promise<<%=Util.genInterfaceName(apiItem.name,"res")%>> {
  let result = (await sdk.get("<%=apiItem.url%>", {
      <% apiItem.requestParam.forEach(paramItem=>{ %>
        <%=paramItem.name%>,
      <% }) %>
  })) as  IResult<IAreaItem[]> ;
  return  result.data;
}
<% }) %>


export default {
<% webapiGroup.apis.forEach(apiItem=>{ %>
    <%=apiItem.name%>,
<% }) %>
};
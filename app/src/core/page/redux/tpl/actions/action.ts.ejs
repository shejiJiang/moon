import {Command} from "@/pages/bankcard-info/constant";
import {Dispatch} from "@/typings";

/**
 * @desc
 *
 * @使用场景
 *
 * @company qianmi.com
 * @Date    2019/4/15
 **/
export default (dispatch:Dispatch) => {

  let subAction = {


    <% action.methods.forEach(method=>{ %>
        /**
         * <%= method.comment||"" %>
         */
        async <%=method.name%>() {
                    //dispatch({ type: INIT, payload: trade });
         },
      <% }) %>
  };
  return subAction;
};

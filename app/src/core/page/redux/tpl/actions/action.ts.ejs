import {Command} from "@/pages/bankcard-info/constant";
import {Dispatch} from "@/typings";

/**
 * @desc
 *
 * @使用场景
 *
 * @company qianmi.com
 * @Date    2019/4/15
 **/
export default (dispatch:Dispatch) => {

  let subAction = {
    async unlockTest(a:string,b:string):Promise<boolean> {
      console.log('unlockTest',a,b);
      return false;
    }
  };
  return subAction;
};

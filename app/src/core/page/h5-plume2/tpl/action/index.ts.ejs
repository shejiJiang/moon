// import Main from './main';

 <% pageInfo.actions.forEach(item=>{ %>
 import <%=Util.toUCamelize(item.fileName)%> from './<%=item.fileName%>';
 <% }) %>

export default {
 <% pageInfo.actions.forEach(item=>{ %>
  <%=Util.toLCamelize(item.fileName)%>:<%=Util.toUCamelize(item.fileName)%>,
 <% }) %>
};